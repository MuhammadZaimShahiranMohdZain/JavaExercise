import static org.junit.Assert.*;

import org.junit.Test;

//import BetterProgrammerTask.Node;

public class BetterProgrammerTaskTest {

	@Test
	public void test() {
		String[] input={"badrul","zaim","akmal"};
		BetterProgrammerTask better = new BetterProgrammerTask();
//		Node root;
//		better.getAverage(root);
		for (int i = 0; i < input.length; i++) {
			System.out.println(input[i]);
		}
	}
}
